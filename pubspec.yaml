name: datepicker_horizontal
description: "A simple and customizable datepicker horizontal for Flutter."
version: 0.0.1

homepage: https://github.com/gunners-pro/datepicker_horizontal
repository: https://github.com/gunners-pro/datepicker_horizontal
issue_tracker: https://github.com/gunners-pro/datepicker_horizontal/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
